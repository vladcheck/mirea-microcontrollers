МИНИСТЕРСТВО НАУКИ И ВЫСШЕГО ОБРАЗОВАНИЯ РОССИЙСКОЙ ФЕДЕРАЦИИ

ПРАКТИЧЕСКАЯ РАБОТА № 1
«Микроконтроллерная схема ввода-вывода на STM32F1»
(схема ввода-вывода, программа на языке Си, получение файла прошивки и моделирование)

Выполнил: студент группы ЭФБО-04-24
Валекжанин Владимир Сергеевич

Проверил: ______________________

==========================================================

1. ТЕМА И ЦЕЛЬ РАБОТЫ

Тема: создание микроконтроллерной схемы ввода-вывода и проверка передачи
состояния входного ключа на светодиодный выход.

Цель работы:
— освоить построение схемы ввода-вывода на микроконтроллере семейства STM32F1;
— научиться настраивать выводы GPIO через регистры (RCC, CRL, IDR, BSRR);
— написать и скомпилировать программу на языке Си, получить файл прошивки .hex;
— подключить файл прошивки к модели микроконтроллера и проверить работу
  схемы при подаче на вход логического нуля и логической единицы.

Общая логика работы схемы во всех вариантах одинакова:

    ключ → вход микроконтроллера → программа → выход микроконтроллера → светодиод

При подаче логической единицы на вход микроконтроллера светодиод включается,
при подаче логического нуля — выключается (активный высокий уровень).

==========================================================

2. КРАТКОЕ ОПИСАНИЕ СРЕДЫ PROTEUS

Proteus — среда схемотехнического моделирования, позволяющая разрабатывать
принципиальные схемы микроконтроллерных устройств и выполнять их отладку
в виртуальном режиме. В Proteus размещаются модели компонентов (микроконтроллер,
ключ, светодиод, резистор), строится электрическая схема, а в свойствах модели
микроконтроллера в поле Program File указывается файл прошивки .hex, полученный
в среде разработки. Это позволяет проверить работу программы до изготовления
реального устройства.

==========================================================

3. КРАТКОЕ ОПИСАНИЕ ИСПОЛЬЗУЕМОЙ СРЕДЫ РАЗРАБОТКИ

Для подготовки программы STM32 используется среда CooCox CoIDE.
Проект создаётся командой Project → New Project, после чего выбирается
микроконтроллер и подключаются компоненты проекта:

— CMSIS (описания регистров и базовые средства работы с микроконтроллером);
— CMSIS BOOT (загрузочный код);
— RCC (включение тактирования периферийных модулей);
— GPIO (настройка портов ввода-вывода).

Если среда не находит компилятор, путь к GNU Arm Embedded Toolchain задаётся
командой Project → Select Toolchain Path. Сборка проекта выполняется командой
Project → Build (клавиша F7), после успешной сборки в папке проекта
(например, Debug) формируется файл прошивки .hex.

==========================================================

4. ВЫБРАННЫЙ МИКРОКОНТРОЛЛЕР

Микроконтроллер: STM32F103C8 (семейство STM32F1, корпус LQFP-48,
ядро ARM Cortex-M3, тактовая частота до 72 МГц).

Индивидуальный вариант: 4 (вариант А — STM32F1, таблица 9.3 методички).

==========================================================

5. ПЕРЕЧЕНЬ ИСПОЛЬЗОВАННЫХ КОМПОНЕНТОВ

— Микроконтроллер STM32F103C8 — 1 шт.;
— ключ-переключатель SW-SPDT — 1 шт. (учебный пример) / 1 шт. (вариант 4);
— светодиод LED — 1 шт. (учебный пример) / 1 шт. (вариант 4);
— резистор 270 Ом (токоограничивающий) — 1 шт. (учебный пример) / 1 шт. (вариант 4);
— цепи питания POWER (3V3) и GND (общий провод).

Все выводы микроконтроллера, не задействованные в схеме, оставлены
незадействованными; питание и общий провод подключены к соответствующим
выводам (VDD, VSS, VDDA, VSSA).

==========================================================

6. СХЕМА МИКРОКОНТРОЛЛЕРНОГО УСТРОЙСТВА

Учебный пример: входной ключ подключён к выводу PA0, светодиод через
токоограничивающий резистор 270 Ом — к выводу PA1 (активный высокий уровень).

INSERT IMAGE ./scheme-example.png HERE
Рисунок 1 — Схема подключения ключа к PA0 и светодиода к PA1 (учебный пример)

Индивидуальный вариант 4: входной ключ подключён к выводу PA6, светодиод
через токоограничивающий резистор 270 Ом — к выводу PA7 (активный высокий
уровень).

INSERT IMAGE ./scheme-variant4.png HERE
Рисунок 2 — Схема подключения ключа к PA6 и светодиода к PA7 (вариант 4)

При верхнем положении переключателя на входной вывод поступает логическая
единица, при нижнем — логический ноль. Программа считывает состояние входа
и устанавливает такое же состояние на выходе. Светодиод загорается при
логической единице на выходном выводе.

==========================================================

7. НАЗНАЧЕНИЕ ВХОДНОГО И ВЫХОДНОГО ВЫВОДОВ

Учебный пример (листинг 9.1 методички):
— вход:  PA0 — чтение состояния ключа (плавающий вход, MODE0=00, CNF0=01);
— выход: PA1 — управление светодиодом (выход push-pull 2 МГц, MODE1=10, CNF1=00).

Индивидуальный вариант 4 (таблица 9.3 методички):

Номер варианта | Входной вывод | Выходной вывод | Используемые порты | Регистр настройки
       4       |      PA6      |      PA7       |       GPIOA        |    GPIOA->CRL

— вход:  PA6 — чтение состояния ключа (плавающий вход, MODE6=00, CNF6=01);
— выход: PA7 — управление светодиодом (выход push-pull 2 МГц, MODE7=10, CNF7=00).

Оба вывода (6 и 7) относятся к младшим линиям порта GPIOA (номер < 8),
поэтому их настройка выполняется в регистре GPIOA->CRL.

==========================================================

8. ТЕКСТ ПРОГРАММЫ НА ЯЗЫКЕ СИ

8.1. Учебный пример — передача состояния входа PA0 на выход PA1 (листинг 9.1)

Листинг 1 — Передача состояния входа PA0 на выход PA1 для STM32F1

#include "stm32f10x.h"

#ifndef SET_BIT
#define SET_BIT(REG, BIT)     ((REG) |= (BIT))
#endif

#ifndef CLEAR_BIT
#define CLEAR_BIT(REG, BIT)   ((REG) &= ~(BIT))
#endif

#ifndef READ_BIT
#define READ_BIT(REG, BIT)    ((REG) & (BIT))
#endif

#ifndef MODIFY_REG
#define MODIFY_REG(REG, CLEARMASK, SETMASK) \
    do {                                     \
        CLEAR_BIT((REG), (CLEARMASK));       \
        SET_BIT((REG), (SETMASK));           \
    } while (0)
#endif

int main(void)
{
    /* Включение тактирования порта GPIOA */
    SET_BIT(RCC->APB2ENR, RCC_APB2ENR_IOPAEN);

    /* PA0 — вход: MODE0 = 00, CNF0 = 01 (плавающий вход) */
    MODIFY_REG(GPIOA->CRL,
               GPIO_CRL_MODE0 | GPIO_CRL_CNF0,
               GPIO_CRL_CNF0_0);

    /* PA1 — выход push-pull 2 МГц: MODE1 = 10, CNF1 = 00 */
    MODIFY_REG(GPIOA->CRL,
               GPIO_CRL_MODE1 | GPIO_CRL_CNF1,
               GPIO_CRL_MODE1_1);

    while (1)
    {
        if (READ_BIT(GPIOA->IDR, GPIO_IDR_IDR0) != 0U)
        {
            SET_BIT(GPIOA->BSRR, GPIO_BSRR_BS1);
        }
        else
        {
            SET_BIT(GPIOA->BSRR, GPIO_BSRR_BR1);
        }
    }
}

В программе используются макросы CMSIS-подобного стиля: SET_BIT устанавливает
выбранные биты регистра, CLEAR_BIT очищает выбранные биты, READ_BIT проверяет
состояние заданного бита, MODIFY_REG изменяет только заданное поле регистра.
Регистр RCC->APB2ENR включает тактирование порта GPIOA, регистр GPIOA->CRL
задаёт режимы выводов PA0–PA7, регистр GPIOA->IDR используется для чтения
состояния входа, регистр GPIOA->BSRR — для установки и сброса выходного уровня.

8.2. Индивидуальный вариант 4 — передача состояния входа PA6 на выход PA7

Листинг 2 — Передача состояния входа PA6 на выход PA7 для STM32F1 (вариант 4)

#include "stm32f10x.h"

#ifndef SET_BIT
#define SET_BIT(REG, BIT)     ((REG) |= (BIT))
#endif

#ifndef CLEAR_BIT
#define CLEAR_BIT(REG, BIT)   ((REG) &= ~(BIT))
#endif

#ifndef READ_BIT
#define READ_BIT(REG, BIT)    ((REG) & (BIT))
#endif

#ifndef MODIFY_REG
#define MODIFY_REG(REG, CLEARMASK, SETMASK) \
    do {                                     \
        CLEAR_BIT((REG), (CLEARMASK));       \
        SET_BIT((REG), (SETMASK));           \
    } while (0)
#endif

int main(void)
{
    /*
     * Включение тактирования порта GPIOA.
     * В варианте 4 используется только порт GPIOA,
     * поэтому достаточно установить бит IOPAEN.
     */
    SET_BIT(RCC->APB2ENR, RCC_APB2ENR_IOPAEN);

    /*
     * PA6 — входной вывод (ключ).
     * MODE6 = 00: режим входа.
     * CNF6  = 01: плавающий вход.
     * Переключатель SW-SPDT явно соединяет вход PA6 либо с цепью
     * питания (лог. 1), либо с общим проводом (лог. 0), поэтому
     * внутренняя подтяжка не требуется.
     */
    MODIFY_REG(GPIOA->CRL,
               GPIO_CRL_MODE6 | GPIO_CRL_CNF6,
               GPIO_CRL_CNF6_0);

    /*
     * PA7 — выход push-pull со скоростью 2 МГц (светодиод).
     * MODE7 = 10: выход 2 МГц.
     * CNF7  = 00: выход общего назначения push-pull.
     */
    MODIFY_REG(GPIOA->CRL,
               GPIO_CRL_MODE7 | GPIO_CRL_CNF7,
               GPIO_CRL_MODE7_1);

    while (1)
    {
        /* Если на входе PA6 логическая единица — включаем PA7,
           иначе — выключаем PA7 */
        if (READ_BIT(GPIOA->IDR, GPIO_IDR_IDR6) != 0U)
        {
            SET_BIT(GPIOA->BSRR, GPIO_BSRR_BS7);
        }
        else
        {
            SET_BIT(GPIOA->BSRR, GPIO_BSRR_BR7);
        }
    }
}

Фрагмент включения тактирования порта (одинаков для обоих случаев, так как
используется только порт GPIOA):

    SET_BIT(RCC->APB2ENR, RCC_APB2ENR_IOPAEN);

Фрагменты настройки входа и выхода для варианта 4:

    MODIFY_REG(GPIOA->CRL,
               GPIO_CRL_MODE6 | GPIO_CRL_CNF6,
               GPIO_CRL_CNF6_0);     /* PA6 — плавающий вход */

    MODIFY_REG(GPIOA->CRL,
               GPIO_CRL_MODE7 | GPIO_CRL_CNF7,
               GPIO_CRL_MODE7_1);    /* PA7 — выход push-pull 2 МГц */

==========================================================

9. ПОЛУЧЕНИЕ ФАЙЛА ПРОШИВКИ .HEX

После ввода программы проект собирается командой Project → Build (F7).
При успешной сборке в окне сообщений появляется сообщение об успешном
завершении компиляции, а в папке проекта формируется файл прошивки.

Имя (путь) к полученному файлу прошивки:
...\PR1_STM32_GPIO_IO\Debug\PR1_STM32_GPIO_IO.hex

INSERT IMAGE ./build-success.png HERE
Рисунок 3 — Сообщение об успешной сборке проекта и расположение файла прошивки

Если файл .hex не появился, его можно получить из .elf утилитой
arm-none-eabi-objcopy:

    arm-none-eabi-objcopy -O ihex PR1_STM32_GPIO_IO.elf PR1_STM32_GPIO_IO.hex

==========================================================

10. ПОДКЛЮЧЕНИЕ HEX-ФАЙЛА В PROTEUS

Свойства модели микроконтроллера открываются двойным щелчком по его
обозначению на схеме. В окне свойств в поле Program File указывается путь
к полученному файлу .hex.

Перед запуском моделирования проверено:
— модель микроконтроллера в Proteus совпадает с моделью, выбранной в CooCox CoIDE;
— правильно указан путь к файлу прошивки;
— файл получен после последней сборки проекта;
— выводы схемы соответствуют выводам в программе (PA0/PA1 — пример, PA6/PA7 — вариант 4);
— задана корректная частота микроконтроллера.

INSERT IMAGE ./program-file.png HERE
Рисунок 4 — Указание файла прошивки в поле Program File модели STM32

==========================================================

11. РЕЗУЛЬТАТ МОДЕЛИРОВАНИЯ

INSERT IMAGE ./sim-input1.png HERE
Рисунок 5 — Результат моделирования при подаче логической единицы на вход

INSERT IMAGE ./sim-input0.png HERE
Рисунок 6 — Результат моделирования при подаче логического нуля на вход

При переключении ключа изменяется состояние входного вывода (PA0 — пример,
PA6 — вариант 4). Программа считывает это состояние и передаёт его на выход
(PA1 — пример, PA7 — вариант 4). Светодиод включается при подаче логической
единицы на вход и выключается при подаче логического нуля, что соответствует
схеме с активным высоким уровнем.

Результаты моделирования для варианта 4:
— вход PA6 = 1 → выход PA7 = 1 → светодиод включён;
— вход PA6 = 0 → выход PA7 = 0 → светодиод выключен.

==========================================================

12. ОШИБКИ, ВОЗНИКШИЕ ПРИ ВЫПОЛНЕНИИ РАБОТЫ, И СПОСОБЫ ИХ УСТРАНЕНИЯ

1. Несоответствие выводов микроконтроллера в схеме и в программе.
   Устранение: перед сборкой сверены обозначения выводов в схеме и в коде
   (вариант 4: вход PA6, выход PA7 — оба в порту GPIOA, регистр CRL).

2. Риск нарушения настроек соседних выводов при записи в регистр CRL.
   Устранение: вместо прямой записи полного значения использован макрос
   MODIFY_REG, который изменяет только нужные поля MODE6/CNF6 и MODE7/CNF7.

3. Забытое включение тактирования порта GPIO при первоначальной отладке —
   порт не реагировал на изменение регистров конфигурации.
   Устранение: в начало программы добавлена строка
   SET_BIT(RCC->APB2ENR, RCC_APB2ENR_IOPAEN);

4. Ошибки ERC (контроль электрических правил схемы) при размещении
   компонентов: неподключённые выводы.
   Устранение: неиспользуемые выводы микроконтроллера помечены флагами
   «не подключено»; все цепи питания и общего провода пронумерованы.

==========================================================

13. ОТВЕТЫ НА КОНТРОЛЬНЫЕ ВОПРОСЫ

1. Для чего используется среда Proteus при разработке микроконтроллерных
   устройств?
   Для схемотехнического моделирования: разработки принципиальной схемы,
   загрузки файла прошивки в модель микроконтроллера и проверки работы
   устройства до изготовления макета.

2. Почему для моделирования микроконтроллера недостаточно одной
   принципиальной схемы?
   Принципиальная схема описывает только соединения компонентов. Для работы
   модели микроконтроллеру требуется программа (файл прошивки .hex),
   которая определяет его поведение.

3. Какой файл необходимо указать в свойствах микроконтроллера Proteus?
   Файл прошивки с расширением .hex (либо .elf, если модель поддерживает
   его загрузку).

4. Для чего используется поле Program File?
   В это поле указывается путь к файлу прошивки, который загружается
   в модель микроконтроллера при запуске моделирования.

5. Почему модель микроконтроллера в Proteus должна соответствовать
   микроконтроллеру, выбранному в среде разработки?
   Программа собирается под конкретную модель (объём памяти, карта
   периферии, выводы). Расхождение моделей может привести к некорректной
   работе или невозможности запуска моделирования.

6. Какой вывод используется в варианте STM32 как вход?  — PA6 (вариант 4).
7. Какой вывод используется в варианте STM32 как выход? — PA7 (вариант 4).
8. Какой вывод используется в варианте ATmega32 как вход? — PB0 (вариант 1
   таблицы 9.4; в рамках данной работы использовался вариант А — STM32).
9. Какой вывод используется в варианте ATmega32 как выход? — PB1 (вариант 1
   таблицы 9.4; в рамках данной работы использовался вариант А — STM32).

10. Для чего в STM32 необходимо включать тактирование порта GPIO?
    Тактовый сигнал разрешает работу периферийного модуля: без включения
    тактирования через RCC->APB2ENR записи в регистры порта не действуют,
    и порт не работает.

11. Какой регистр STM32 используется для чтения состояния входных выводов?
    Регистр GPIOx_IDR (input data register).

12. Почему для установки и сброса выходного уровня в STM32 удобно
    использовать регистр BSRR?
    BSRR (bit set/reset register) позволяет атомарно установить или сбросить
    выбранные биты одной записью, без операции чтения-модификации-записи
    и риска гонки (порча соседних битов невозможна).

13. Какую функцию выполняет регистр DDRx в ATmega32?
    Задаёт направление работы линий порта: 1 — выход, 0 — вход.

14. Чем отличается регистр PORTx от регистра PINx в ATmega32?
    PORTx задаёт состояние выходных линий (и включает внутреннюю подтяжку
    для входов), а PINx используется для чтения фактического состояния
    линий порта.

15. Что произойдёт, если светодиод подключить к другому выводу, чем указано
    в программе?
    Программа будет управлять выводом, указанным в коде, а светодиод на
    другом выводе останется неизменным — схема работать не будет.

16. Почему в схеме необходим общий провод?
    Общий провод задаёт нулевой потенциал, относительно которого определяются
    логические уровни. Без него цифровые уровни схемы не имеют общей точки
    отсчёта, и модель работает некорректно.

17. Для чего в цепь светодиода включается резистор?
    Резистор ограничивает ток через светодиод, защищая его и вывод
    микроконтроллера от повреждения.

18. Чем отличается активный высокий уровень включения светодиода от
    активного низкого?
    При активном высоком уровне светодиод светится при логической единице
    на выходе; при активном низком — при логическом нуле (светодиод
    подключён между выводом и питанием), работа визуально обратная.

19. Почему при использовании кнопки может потребоваться подтягивающий
    резистор?
    Кнопка с одним контактом соединяет вход только с одним уровнем; когда
    кнопка отпущена, вход «плавает» и принимает случайные значения.
    Подтягивающий резистор (внешний или внутренний) задаёт определённый
    уровень в этом состоянии.

20. Как проверить, что файл .hex сформирован после последней компиляции?
    По времени создания файла (должно быть позже последней сборки) и по
    сообщению об успешной сборке в окне вывода среды разработки; можно
    заново выполнить сборку и убедиться, что файл обновился.

==========================================================

14. ВЫВОД

В ходе практической работы создана микроконтроллерная схема ввода-вывода
на микроконтроллере STM32F103C8: выполнен учебный пример (ключ на входе PA0,
светодиод на выходе PA1) и индивидуальный вариант 4 (ключ на входе PA6,
светодиод на выходе PA7 через резистор 270 Ом). Написана программа на языке
Си, настраивающая вход как плавающий, выход как push-pull, включающая
тактирование порта GPIOA через регистр RCC->APB2ENR и передающая состояние
входа на выход через регистры IDR и BSRR. Проект успешно скомпилирован,
получен файл прошивки .hex, указанный в поле Program File модели
микроконтроллера. Моделирование подтвердило передачу состояния входа на выход:
при подаче логической единицы на вход светодиод включается, при подаче
логического нуля — выключается. Цель работы достигнута.
